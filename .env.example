# ============================================
# Peer Review Hub - Environment Configuration
# ============================================
# Copy this file to .env and customize values
# IMPORTANT: Never commit .env to version control

# Database Configuration
# ----------------------
DB_USER=postgres
DB_PASSWORD=your-secure-password-here    # CHANGE THIS in production!
DB_NAME=peer_review_hub

# JWT Authentication
# ------------------
JWT_SECRET=your-super-secret-jwt-key-change-in-production    # CHANGE THIS! Use: openssl rand -base64 32
JWT_EXPIRES_IN=7d

# Frontend Configuration
# ----------------------
# For local development:
VITE_API_URL=http://localhost:3001
# For production (if frontend is served on same domain):
# VITE_API_URL=https://api.yourdomain.com

# ===========================================
# Production Notes
# ===========================================
#
# 1. Generate a strong JWT_SECRET:
#    openssl rand -base64 32
#
# 2. Use strong database password:
#    openssl rand -base64 24
#
# 3. For production deployment with SSL:
#    - Add reverse proxy (nginx/traefik) with SSL
#    - Update VITE_API_URL to use HTTPS
#
# 4. Docker production command:
#    docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
